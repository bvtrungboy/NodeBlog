{
  "name": "blog",
  "version": "1.0.0",
  "description": "description",
  "main": "src/index.js",
  "scripts": {
    "prettier": "lint-staged",
    "start": "nodemon --inspect src/index.js localhost 3000",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "node-sass -w src/resource/scss -o src/public/css"
  },
  "author": "Boo",
  "license": "MIT",
  "lint-staged": {
    "src/**/*.{js,json,scss}": "prettier --single-quote --trailing-comma --tab-width 4 all --write"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-handlebars": "^5.3.3",
    "husky": "^7.0.1",
    "lint-staged": "^11.1.2",
    "nodemon": "^2.0.12",
    "prettier": "^2.3.2"
  },
  "devDependencies": {
    "morgan": "^1.10.0",
    "node-sass": "^6.0.1"
  }
}
